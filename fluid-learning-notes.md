# 流体算法学习记录（FluidNinja / Niagara Grid 2D / Stable Fluids）

> 2026-08-22 的完整学习对话整理。配套交互教学页：[fluidninja-fluid-tutorial.html](./fluidninja-fluid-tutorial.html)（同目录，双击浏览器打开）
> Obsidian 笔记：`D:\Note\Obsidian\001_笔记库\UE\000_基础知识点\Niagara\流体模拟与 Niagara Curl Noise 算法笔记.md`

---

## 一、总纲：流体不是东西，是场

> **不追踪每一滴水，而是把空间切成网格，每个格子记录一缕"风"（速度向量），让固定流水线每帧轮流上场。**

- **粒子法（拉格朗日）**：跟着每滴水旅行（点在动）。Niagara 普通发射器属于这类。
- **网格法（欧拉）**：站在每个格子看天气（格子不动，读数在变）。FluidNinja / Niagara Grid 2D 属于这类。
- 流体的全部状态 = **两张表（场）**：速度场（每格一个箭头）+ 染料场（每格多少墨）。
- 格子像 LED 大屏的像素：**焊死不动，亮度和颜色（数值）在变**——"流动"是数值分布的挪位（人浪），没有任何东西真的在移动。
- 判断 Niagara 形态：数据在 Particles 里 = 粒子法；数据在 Render Target / Grid Collection 里 = 网格法。

**核心循环：速度场搬运染料，物理规律修正速度场。**

---

## 二、一帧流水线（三步 + 调味）

```
① 注入 → ② 配平 → ③ 平流（搬运）→ 下一帧……（每秒 60 圈）
```

### ① 注入（外力点火）

在某点注入速度，**全场每个格子独立计算**"我离注入点多远 → 按高斯折扣分到多少力 → 加进自己"。

两个旋钮：
- **力度**：这次划水多大劲（乘在最前面，全体按比例）
- **半径**：折扣表多慷慨（半径小=针刺，大=船桨划水）

手算例（3 格 A/B/C，注入向右力度 2，折扣 100%/10%/10%）：
```
注入后速度：A 0.2 →   B 2.0 →   C 0.2 →
```
墨同理：B 得 1.0，A/C 各 0.1。

要点：
- 格子之间**从不通信**，各算各的（GPU 并行）
- 注入点影响的是**场**，不是直接推别的点——一切影响经场中转（点→场→点，没有点对点直连）
- 你的输入只是"愿望"，配平把它修正成"物理允许的事"

### ② 配平（压力投影）——算法的心脏

**查账（差异/散度）**：水不可压缩 → 每格"流入 = 流出"。盯"格子之间的门"：

手算例（延续上面）：
| 格子 | 收进 | 送出 | 账 |
| --- | --- | --- | --- |
| A | 0（左墙） | 0.2 | 亏 0.2 |
| B | 0.2 | 2.0 | **亏 1.8** |
| C | 2.0 | 0（右墙） | **盈 2.0** |

亏空=水凭空消失、暴涨=凭空变多——物理不允许，必须修。

**修法（压力迭代 / Jacobi）**：一条蠢规则刷 20~40 遍：
```
p[自己] = (p左 + p右 + p上 + p下 − div) / 4
```
- "我 = 邻居平均 − 我的账"
- **没有总指挥**：每个格子只看邻居微调自己（像全班排队，每人只看左右邻居，几十轮后全场自动排齐——秩序从局部规则涌现）
- 一轮配不平（牵一发动全身），所以迭代；游戏敢只刷 20~40 次（"几乎平"视觉上已是水）
- **热启动**：上一帧压力留 80% 当起点（趋势有惯性，收敛快）

**执行（梯度减除）**：`u = u − ∇p`（逆着压力差修速度）。修正后散度=0 ✓。

手算例修正结果（示意）：B 2.0 → 1.3，A/C 0.2 → 0.55（B 多余的速度被分还给两边——你注入的是冲劲，水表现出的是被摊匀的合法状态）。

### ③ 平流（半拉格朗日 · 惯性搬运）

> **平流 = 墨被风带着晕开——像一滴墨滴进水里，云雾一样慢慢散出去。**

算法的问法是反的：不问"我的东西去哪"，问"**谁流进了我？它上次在哪？**"——沿自己的速度**倒退**一步，去上游把旧值抄回来。

手算例（dt=0.5 秒）：
```
C 的墨：速度 0.2 → 倒退 0.1 格 → 落在 B、C 之间（一成靠 B、九成靠 C）
        新墨 = B 的墨 1.0×10% + C 的墨 0.1×90% = 0.19  ← 墨涌进 C ✓
B 的墨：速度 2.0 → 倒退 1 格 → 正好落在 A 的格心
        新墨 = A 的墨 0.1  ← 大墨团走了 ✓
```

- **倒退抄值 = 稳定的来源**：正着"撒"会撒丢（质量不守恒、爆掉），倒着"抄"永远只是取一个数
- 平流每帧产出两张新表：新速度（风运风=惯性延续）+ 新墨（墨搭风的车）
- **墨自己没有惯性**，有惯性的是风；惯性认"此刻的速度"，不认"初始速度"（滚动惯性）
- 惯性不存储、每帧实时从场现读现用——所以实时交互零延迟、衰减才能逐帧折损它
- 平流不问风的来历（快递员不管包裹为什么寄）——有风就搬

### 配平 vs 平流：推歪-扶正的搭档

```
平流：搬家 → 造出新差异（弄乱）    ← 内生的乱（家里人走动碰乱东西）
配平：解压力 → 熨平（整理）        ← 保洁员，见乱就整，不问谁弄的
每帧循环，谁也不闲着
```
- 新差异 ≠ 新的注入（它是平流的副产品，场内部自己长出来的）
- 新差异**解出**新的压力（差异是题目，压力是答案——不是"就是"）
- **注入只需点火一次，环靠"平流造乱、配平熨平"自我维持**
- 配平不消耗能量（重新分配），衰减才消耗（真实折损）
- 配平管"像不像水"，衰减管"什么时候停"

### 衰减（寿命）

每帧 `值 /= (1 + 衰减×dt)`。烟大、墨 0、水微小。是美术参数不是物理。

---

## 三、因果环（整套算法的世界观）

> **在某一点注入速度，场会在空间上产生差异；差异解出压力，压力化为流动，流动搬运一切、又制造新的差异——流体的全部现象，就是这个循环的持续运转。**

```
注入（点火）→ 差异（出题）→ 压力（解题）→ 修正 → 平流（惯性搬运+弄出新乱）→ 新差异 → ……循环
```

三种结局：
| 情形 | 结局 |
| --- | --- |
| 点火一次 + 有衰减 | 能量磨光 → 回到全零空转（湖面复静） |
| 点火一次 + 无衰减 | 近乎永转 |
| **持续点火** | 永动（河流进水口、火焰火源——每帧注入） |

全零时循环照转，只是空转（平流=每格抄自己）。**循环一直在转，注入决定它空转还是实转。**

类比（湖与河）：湖面静=账平；高低落差=差异；水往低处流=压力驱动；上游来水=注入；压力释放口=outflow。浅水方程模型甚至直接拿"水面高度"当压力。

---

## 四、纳维-斯托克斯方程（符号表）

```
∂u/∂t = −(u·∇)u − (1/ρ)∇p + ν∇²u + f
∇·u = 0
```

| 符号 | 读法 | 意思 | 对应概念 |
| --- | --- | --- | --- |
| ∂u/∂t | u 对 t 的偏导 | 速度随时间的变化 | 这一帧速度怎么变 |
| u | 速度场 | 每格的箭头 | 风 |
| ∇ | nabla | 梯度算子(∂/∂x, ∂/∂y) | 差异探针 |
| −(u·∇)u | 平流项 | 风运风 | **平流（惯性）** |
| (1/ρ)∇p | 压力项 | 压力差推动 | **配平产生的流动** |
| ρ | rho | 密度（常数） | 比例系数 |
| ∇² | 拉普拉斯 | 和邻居平均差多少（抹平倾向） | 抹平器 |
| ν | nu | 运动粘度 | （游戏扔掉此项，数值耗散免费送） |
| f | 外力 | 外界加的力 | **注入** |

白话：**速度的变化 = 惯性搬运 + 压力推动 + 粘性抹平(扔) + 外力注入。**

（完整公式版算法 + 每步离散公式见对话末尾 / 教学页附录。）

---

## 五、涡流（进行中 · 已学第一小步）

**涡 = 格子里的水在原地打转。测量工具：想象放一个小桨轮（十字叶片）。**

旋转场的布置（顺时针）：
```
上邻 →(+1,0)   右邻 ↓(0,−1)
左邻 ↑(0,+1)   下邻 ←(−1,0)
```
桨轮四臂被同向搓 → 顺时针转 ✓

**涡度 =（右的竖直 − 左的竖直）−（上的水平 − 下的水平）**
手算：=(−1−1)−(1−(−1)) = −4 → 负=顺时针，绝对值=强度

对照（齐步流，全往东）：=(0−0)−(1−1)=0 → 桨轮平移不转 → 直流无涡 ✓
这就是"流动"和"旋转"的数学分界线。

**待学**（下次继续）：
- 涡怎么出生：速度差（剪切）搓出来——左手慢右手快搓汤圆；注入（手指下快边缘慢）和障碍（石头前停两侧快）都是速度差制造机
- 涡怎么被看见：旋转的速度场搬运墨，墨显影出螺旋
- 涡的一生：出生→平流拉伸成形→惯性自我维持→衰减+数值耗散磨损→死亡
- 涡度约束：检测快被磨死的涡，沿 ∇|ω| 方向补力续命（Fedkiw 1996，非物理的游戏技巧）；教学页演示 3-1 滑块 0=蜂蜜 / 40=湍流
- 卡门涡街：河流演示石头背后周期性脱落的涡（工程要防，美术白嫖）

---

## 六、Niagara 相关

- **Grid 2D 一帧**：Simulation Stages 依次 = 注入(Source/Set Grid Values) → 外力(Apply Forces) → 散度(Divergence) → 压力(Pressure，Num Iterations 参数在这) → 梯度 → 平流(Advect) → 输出(Render Parameters)
- **材质 vs 蓝图**：模拟在 Simulation Stages（算）；材质采样输出 RT（画）；蓝图只在游戏逻辑时进场（传玩家位置、触发事件）。Niagara→材质经 User Parameters 直连，无蓝图
- **场的初始化**：Spawn 时分配纹理清零（买空账本），内容每帧滚动重写；能预写的只有烘焙 Vector Field（另一条路）
- **纯材质流体**：材质无状态→只能伪造（flow map/panner）；材质+RT 乒乓可跑真模拟但每帧 1 pass（做不了完整压力迭代）；Ninja/Niagara = 每帧十几个 pass 的原生调度
- **Curl Noise**（已单独整理进 Obsidian 笔记）：势函数取旋度→数学保证无散；等高线比喻（坡度转90°）；Niagara 源码 `NiagaraDataInterfaceCurlNoise.cpp`，每粒子每帧约 18 次 3D simplex 采样
- **粒子流体感三档**：curl noise 伪装（扰动不传播）/ 网格场驱动（推荐，运动就是流体的运动）/ SPH（全真但贵）。三问自检：推一下传开吗？两涡互相影响吗？粒子穿插堆积吗？

---

## 七、概念纠偏档案（一路踩过的坑，最珍贵的部分）

| 曾经的误解 | 真相 |
| --- | --- |
| "差异就是平流" | 差异是配平的**题目**；平流是搬运，在环的第四节。原句现象对、标签错 |
| "平流是初始速度和方向" | 那是注入的风。平流=用现成的风搬东西（风靠自己惯，墨搭风的车） |
| "平流是粒子运动的过程" | 没有粒子动，是数值人浪：格子原地不动抄上游的数 |
| "粒子因初始惯性流动" | 风有惯性（滚动的），墨没有惯性（被搬的）——"初始"改"此刻" |
| "惯性是记录下来的" | 惯性不存储，每帧从场现读（速度场的当前值就是惯性） |
| "新的差异=新的输入" | 新差异是平流弄出的内生的乱，不是外部注入 |
| "新的差异就是新的压力" | 差异出题，压力解题——"解出"不是"就是" |
| "平流=配平现象" | 两个不同步骤：配平=熨平（整理），平流=搬运；顺序：先配平再平流 |
| "场是根据点的移动来的" | 半拉格朗日每帧每格临时雇一个"替身点"倒退一步——借用点的视角，不追踪点 |
| "风是具体的东西" | 风不是实体，是流动本身；速度场每帧被"注入+压力+平流"现做 |

**最终世界观定稿：**
> 流体是场：外力注入速度 → 空间差异逼出压力 → 压力投影塑造合法流动 → 速度场平流自己和乘客 → 新差异 → 循环，衰减定终局。
> 注入给惯性，平流花惯性，衰减收惯性。

---

## 八、公式版算法（可复制）

见教学页第 8 章与附录，或直接用极简版：

```
① u += f                                    （注入）
② 涡度补力: u += ε(∇|ω|×ω)dt               （续命，可选）
③ div = (R−L+T−B)/2                         （查账）
④ p = (四邻p之和−div)/4  ×N 次               （解题，热启动 p×0.8）
⑤ u −= ∇p                                   （修正）
⑥ u,d = 旧场在 (x − dt·u) 处的插值           （平流：倒退抄值）
⑦ u,d /= (1+k·dt)                           （衰减）
```

---

*学习方式备注：从三个格子手算开始 → 因果环 → 引擎落地。所有概念都在教学页有可玩的演示对应（ch1 场 / ch2 平流 / ch3 涡度 / ch4 配平 / ch5 总装 / ch6 火焰河流）。下一步：涡流第二小步（涡的出生：速度差搓转）。*
